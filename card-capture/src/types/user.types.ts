export type ProductCategory = {
  productCategory: string;
  quantity: number;
};

export type User = {
  email: string;
  verified_email: boolean;
  name: string;
  given_name: string;
  family_name: string;
  picture: string;
  created_at: string;
  updated_at: string;
  id: string;
};
